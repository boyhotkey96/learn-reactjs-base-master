const initProducts = {};

const productsReducer = (state = initProducts, action) => {
  switch (action.type) {
    case '...':
      break;
    default:
      return state;
  }
};

export default productsReducer;
